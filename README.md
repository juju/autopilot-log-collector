Simple log collection script intendened to aid autopilot debugging.  Can be
used to collect logs from any Juju environment, though.

To run:

    ./collect-logs /path/log-file-name.tar.gz

To test:

    make test
